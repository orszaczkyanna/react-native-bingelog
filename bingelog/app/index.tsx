// Onboarding Screen

import React from "react";
import { Text } from "react-native";
import ScreenWrapper from "@/components/ScreenWrapper";

const index = () => {
  return (
    <ScreenWrapper>
      <Text className="text-body">index</Text>
    </ScreenWrapper>
  );
};

export default index;
